#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n; cin>>n;
    int tmp = n*(n+1)/2;
    cout << tmp << '\n' << (int)pow(tmp, 2) << '\n' << (int)pow(tmp, 2);


}
